<%= form_for(cc_view) do |f| %>
  <% if cc_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cc_view.errors.count, "error") %> prohibited this cc_view from being saved:</h2>

      <ul>
      <% cc_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %>
    <%= f.number_field :id %>
  </div>

  <div class="field">
    <%= f.label :display %>
    <%= f.text_field :display %>
  </div>

  <div class="field">
    <%= f.label :default %>
    <%= f.check_box :default %>
  </div>

  <div class="field">
    <%= f.label :locked %>
    <%= f.check_box :locked %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
