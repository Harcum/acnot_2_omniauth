<%= form_for(informed_person) do |f| %>
  <% if informed_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(informed_person.errors.count, "error") %> prohibited this informed_person from being saved:</h2>

      <ul>
      <% informed_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.number_field :person_id %>
  </div>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.number_field :student_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
